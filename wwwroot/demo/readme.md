# Demo Assets

Runtime demo data now lives in typed C# services under `Data/DemoDataService.cs`.

Keep this folder only for static, non-runtime demo artifacts if the app needs downloadable sample files later.
